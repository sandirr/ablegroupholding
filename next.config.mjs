// import withPurgeCss from '@fullhuman/postcss-purgecss';

const nextConfig = {
  images: {
    domains: ['placehold.co'],
  },
  i18n: {
    locales: ['en', 'id', 'zh'],
    defaultLocale: 'en',
  },
};

export default nextConfig;
